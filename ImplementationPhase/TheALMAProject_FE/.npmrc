# Bắt buộc dùng package-lock.json (không tự update lockfile)
save-exact=true

# Ngăn npm đọc node_modules từ thư mục cha (quan trọng!)
# Mỗi member kéo về chạy "npm ci" thay vì "npm install"
engine-strict=true
